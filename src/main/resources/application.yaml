spring:
  application:
    name: edu-tracker-admin
  datasource:
    url: ${DB_HOST}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
  elasticsearch:
    uris: http://slj-edutracker-elasticsearch:9200

server:
  servlet:
    context-path: /edu-tracker/admin/

minio:
  accessKey: admin
  secretKey: password
  endpoint: http://minio:9000

sendgrid:
  api:
    key: ${SG_KEY}
    from: ${SG_FROM}

